{
  "name": "star",
  "version": "1.0.0",
  "scripts": {
    "generate": "node generate-data.js",
    "watch": "nodemon --watch \"*.html\" --exec node generate-data.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "jsdom": "^26.1.0"
  }
}
